{% extends "::adminPagePanel.html.twig" %}
{% block scripts %}
{% endblock %}
{% block body %}
<div id="sidebar">
    
    <ul class="main-nav">
        {% for m in menu %}
        {% if(active==m.id) %}
            <li class="active"> 
                <a href="{{ path('admin_page_homepage',{'t':m.id}) }}" ><i class="fa {{ m.descripcion }} fa-home"></i> {{ m.nombre }}</a>
            </li>        
        {% else %}
            <li>
                <a href="{{ path('admin_page_homepage',{'t':m.id}) }}" ><i class="fa {{ m.descripcion }} fa-home"></i> {{ m.nombre }}</a>
            </li>
        {% endif %}
        {% endfor %}
    </ul>
</div> <!-- /sidebar -->

<div id="content" class="clearfix">


    <div class="breadcrumbs">
        <i class="fa fa-home"></i> Inicio <i class="fa fa-caret-right"></i> {{ name }}
    </div>

    <div class="wrp clearfix">

        
        <a class="btn icon-button" href="{{ path('admin_page_createReglamento') }}" ><i class="fa fa-edit"></i> Nuevo</a>
        
        <div class="fluid">

            <div class="widget grid12">
                <div class="widget-header">
                    <div class="widget-title">
                        <i class="fa fa-paperclip"></i> {{ name }}
                    </div>
                    <div class="widget-controls">
                        <input type="checkbox" id="switch-ui" />
                        <label class="switch" for="switch-ui"><i></i></label>
                    </div>
                </div> <!-- /widget-header -->

                <div class="widget-content">

     <table style="width:100%;">
        <th style="width:20%;">TÍTULO</th>
        <th style="width:20%;">DESCRIPCIÓN</th>
        <th style="width:20%;">CIRCULAR</th>
        <th style="width:20%;">RESOLUCIÓN</th>
        <th style="width:20%;">REGLAMENTO</th>
         {% for new in news %}
            <tr>
                 <td>{{ new.titulo | raw }}</td>
                 <td>{{ new.descripcion | raw }}</td>
                 <td>{{ new.file_circular | raw }}</td>
                 <td>{{ new.file_resolucion | raw }}</td>
                 <td>{{ new.file_reglamento | raw }}</td>
                 <td><a class="btn icon-button" href="{{ path('admin_page_getReglamento',{ 'id': new.id })  }}"><i class="fa fa-edit fa-sm"></i></a></td>
                 <td><a class="btn icon-button" href="{{ path('admin_page_eliminarReglamento',{ 'id': new.id })  }}"><i class="fa fa-times-circle fa-sm"></i></a></td>
            </tr>    
         {% endfor %}
     </table>    


                </div>

            </div> <!-- /widget -->

        </div> <!-- /fluid -->

    </div> <!-- /wrp -->

</div> <!-- /content -->{% endblock %}


